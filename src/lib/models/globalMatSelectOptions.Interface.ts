export interface GlobalMatSelectOptionsInterface {
  /**
   * @description the text to view in every option
   */
  text  : string;

  /**
   * @description the value to emit on user select
   */
  value : any;
}
